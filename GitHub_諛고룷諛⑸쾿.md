# GitHub Pages로 홈페이지 올리기

돈 들지 않고, 웹 화면에서 바로 수정할 수 있고, 랩원에게 권한도 나눠줄 수 있습니다.
**프로그램 설치 없이 웹 브라우저만으로 끝납니다.**

전체 20분 정도 걸립니다. 한 번만 해두면 그 뒤로는 수정이 훨씬 쉬워집니다.

---

# 1단계 · GitHub 가입 (5분)

1. https://github.com 접속
2. 오른쪽 위 **Sign up** 클릭
3. 이메일, 비밀번호, 아이디를 정합니다
   - **아이디가 홈페이지 주소가 됩니다.** 신중히 정하세요.
   - 예: 아이디를 `needlab-cau` 로 하면 → `needlab-cau.github.io/...`
   - 영어 소문자와 하이픈(-)만 쓰는 것이 안전합니다
4. 이메일로 온 인증 코드를 입력하면 가입 완료

> 연구실 공용 계정으로 만드는 것을 권합니다.
> 개인 계정으로 만들면 그 사람이 졸업할 때 곤란해집니다.

---

# 2단계 · 저장소 만들기 (2분)

"저장소(Repository)"는 홈페이지 파일을 담아두는 폴더입니다.

1. 로그인 후 오른쪽 위 **＋** → **New repository**
2. 아래처럼 입력합니다

   | 항목 | 값 |
   |---|---|
   | Repository name | `needlab` (원하는 이름, 영어 소문자) |
   | Description | 비워둬도 됩니다 |
   | 공개 설정 | **Public** ← 반드시 Public이어야 무료로 홈페이지가 열립니다 |
   | Add a README file | 체크하지 **않음** |

3. 아래 초록색 **Create repository** 클릭

---

# 3단계 · 파일 올리기 (5분)

1. 방금 만든 저장소 화면에서 **uploading an existing file** 링크를 클릭
   (또는 **Add file → Upload files**)

2. **압축을 푼 폴더를 열고, 그 안의 내용물을 전부 선택해서** 드래그합니다.

   > ⚠️ **가장 흔한 실수**
   > 폴더 자체를 드래그하지 말고, **폴더를 열어서 안에 있는 것들**을 드래그하세요.
   >
   > ```
   > 올려야 할 것 (폴더 안의 내용물)
   >   index.html
   >   research.html
   >   ... 나머지 html 파일들
   >   assets      (폴더)
   >   content     (폴더)
   >   images      (폴더)
   >   build-data.py
   >   수정안내서.md
   > ```
   >
   > 크롬에서는 폴더를 통째로 드래그해도 안의 내용이 올라갑니다.
   > 업로드 목록에 `index.html` 이 **맨 앞에 그대로** 보이면 정상입니다.
   > `needlab/index.html` 처럼 폴더 이름이 앞에 붙어 있으면 잘못된 것입니다.

3. 파일이 다 올라오면 아래 **Commit changes** 클릭

---

# 4단계 · 홈페이지 켜기 (3분)

1. 저장소 위쪽 메뉴에서 **Settings** 클릭
2. 왼쪽 목록에서 **Pages** 클릭
3. **Build and deployment** 항목에서

   | 항목 | 선택 |
   |---|---|
   | Source | **Deploy from a branch** |
   | Branch | **main** |
   | 폴더 | **/ (root)** |

4. **Save** 클릭
5. **1~3분 기다립니다.** (첫 배포는 좀 걸립니다)
6. 같은 화면 위쪽에 주소가 나타납니다

   ```
   https://아이디.github.io/needlab/
   ```

   그 주소를 클릭하면 홈페이지가 열립니다. **끝났습니다.**

> 주소를 눌렀는데 404가 뜨면 1~2분 더 기다린 뒤 새로고침하세요.

---

# 5단계 · 내용 수정하기 (앞으로 계속 쓰는 방법)

## 글 고치기 (소식 추가 등)

1. 저장소 첫 화면에서 **content** 폴더 클릭
2. 고칠 파일 클릭 (예: `news.json`)
3. 오른쪽 위 **연필 아이콘 ✏️** 클릭
4. 내용을 고칩니다
5. 아래로 내려 **Commit changes** → 다시 한 번 **Commit changes**
6. **1~2분 뒤 홈페이지에 반영됩니다**

> JSON 형식 규칙은 `수정안내서.md` 2장을 보세요.
> 쉼표 하나만 틀려도 화면이 안 나옵니다.

## 사진 올리기

1. **images** → **uploads** 폴더로 들어갑니다
2. **Add file → Upload files** 클릭
3. 사진을 드래그하고 **Commit changes**
4. 그 다음 `content` 안의 JSON에서 경로를 적어줍니다
   ```
   "image": "images/uploads/사진이름.jpg"
   ```

> 파일 이름은 **영어와 숫자로만** 지으세요. 한글 이름은 오류가 납니다.
> 사진은 올리기 전에 가로 1600px 정도로 줄이세요.

---

# 6단계 · 랩원에게 권한 주기

1. 저장소 → **Settings** → 왼쪽 **Collaborators**
2. **Add people** 클릭
3. 랩원의 GitHub 아이디 또는 이메일 입력 → 초대
4. 초대받은 사람이 메일에서 수락하면 수정할 수 있습니다

> 권한을 줄 때 **`수정안내서.md` 를 꼭 함께 전달**하세요.
> 특히 JSON 3가지 규칙을 읽게 하세요.

## 실수해도 괜찮습니다

GitHub은 모든 수정 기록을 남깁니다.

- 저장소 → **commits** (시계 모양 아이콘) 에서 지금까지의 변경을 볼 수 있습니다
- 잘못된 수정을 클릭 → **Revert** 를 누르면 그 전으로 되돌아갑니다

**무엇을 잘못해도 복구 가능합니다.** 겁내지 말고 고치세요.

---

# 7단계 · 주소를 짧게 만들기 (선택)

## 방법 A — 계정 주소를 그대로 쓰기

저장소 이름을 **`아이디.github.io`** 로 만들면 주소가 짧아집니다.

- 저장소 이름: `needlab-cau.github.io`
- 홈페이지 주소: `https://needlab-cau.github.io/` ← 뒤에 폴더 이름이 안 붙음

이미 만든 저장소 이름은 **Settings → 맨 위 Repository name** 에서 바꿀 수 있습니다.

## 방법 B — 학교 도메인 연결

`needlab.cau.ac.kr` 같은 주소를 쓰려면 학교 전산팀에 요청해야 합니다.

1. 전산팀에 "CNAME 레코드를 `아이디.github.io` 로 연결해 달라"고 요청
2. GitHub 저장소 → **Settings → Pages → Custom domain** 에 그 주소 입력
3. **Enforce HTTPS** 체크

---

# 자주 막히는 지점

| 증상 | 해결 |
|---|---|
| 주소를 눌렀는데 404 | 1~3분 더 기다린 뒤 새로고침. Settings → Pages에서 Branch가 main인지 확인 |
| 글자만 나오고 디자인이 없음 | 파일을 폴더째 올렸습니다. `assets` 폴더가 저장소 첫 화면에 보여야 정상 |
| 수정했는데 그대로 | 1~2분 걸립니다. 그 뒤에도 안 바뀌면 `Ctrl+F5` 로 새로고침 |
| 화면이 아예 안 나옴 | JSON 형식 오류입니다. https://jsonlint.com 에서 확인하세요 |
| Pages 메뉴가 안 보임 | 저장소가 Private입니다. Settings 맨 아래에서 Public으로 바꾸세요 |

---

# 요약 (한 장으로)

```
1. github.com 가입          → 아이디가 곧 주소
2. New repository           → 이름 정하고 Public
3. Upload files             → 폴더 "안의 내용물"을 드래그
4. Settings → Pages         → main / (root) → Save
5. 1~3분 뒤 주소 확인       → 완료

이후 수정: 파일 클릭 → 연필 ✏️ → 고치고 → Commit → 1~2분 뒤 반영
```
